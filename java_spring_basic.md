# **מדריך למידה מקיף: יסודות Java, תכנות מתקדם ושלדת העבודה Spring**

\*מדריך זה סוקר את עקרונות המפתח של שפת Java, החל מהיסודות ועד לנושאים מתקדמים כמו Generics ושלדת העבודה Spring, על בסיס חומרי המקור.

## **עשר שאלות פתוחות קצרות**

**1. מהו ההבדל המרכזי בין "חריגות בדוקות" (Checked Exceptions) ל"חריגות לא בדוקות" (Unchecked Exceptions)?** 

ההבדל המהותי הוא ש**Checked Exceptions עוברות בקרת מהדר בזמן הקומפילציה**, מה שמאלץ את המתכנת לטפל בתקלות צפויות מראש. לעומת זאת, **Unchecked Exceptions אינן נבדקות בזמן ההידור** ונובעות לרוב מליקויי לוגיקה שיש למנוע בקידוד נכון.

**2\. הסבר את תפקידו של ה-JVM (Java Virtual Machine) בתהליך הרצת קוד Java:** 

ה-JVM מתרגם את ה-Bytecode של Java לקוד מכונה ייעודי למערכת ההפעלה. **מנגנון זה הופך את יישומי Java לניידים** ומאפשר הרצה על פלטפורמות שונות כגון Windows או Linux ללא צורך בהתאמות קוד.

**3\. מהי המטרה של ממשק ה-Iterable בהיררכיית ה-Collections?** 

ממשק זה עומד בראש המבנה ו**מגדיר אובייקט המאפשר מעבר סדרתי על איבריו**. הוא מאפשר **שימוש בלולאת for-each תוך הסתרת המימוש הפנימי** של האוסף, מה שמקנה גמישות בשינוי מבנה הנתונים בעתיד.

**4\. תאר את המושג "הזרקת תלויות" (Dependency Injection) וכיצד הוא תורם לעיצוב התוכנה:** 

מדובר בטכניקה של **אספקת אובייקטים נדרשים למחלקה מגורם חיצוני** במקום יצירתם הפנימית בעזרת `new`. הזרקה זו מייצרת **צימוד רופף (Loosely Coupled)** התורם ליכולת התחזוקה והבדיקה של המערכת.

**5\. מהו ההבדל בניהול הזיכרון בין טיפוסים פרימיטיביים (Primitive Types) לטיפוסי ייחוס (Reference Types)?** 

בטיפוסים פרימיטיביים, **הערך נשמר ישירות במיקום הזיכרון** המוקצה. לעומת זאת, טיפוסי ייחוס שומרים רק **מצביע לכתובת האובייקט בזיכרון**, כך שהעתקת משתנה כזה גורמת לשני משתנים להצביע על אותו אובייקט בדיוק.

**6\. מהו "ממשק פונקציונלי" (Functional Interface) וכיצד הוא קשור לביטויי למדא (Lambda Expressions)?** 

זהו **ממשק המכיל מתודה מופשטת אחת בלבד**. בשל כך, **ניתן לממשו בתמציתיות באמצעות Lambda Expressions**, מה שמייתר את הצורך במחלקות פנימיות אנונימיות ומייצר קוד קריא ואלגנטי יותר.

**7\. הסבר את המושג "Syntactic Sugar" בהקשר של לולאת for-each ואיטרטורים:** 

המושג מתייחס לכך ש**לולאת for-each היא מעטפת נוחה המומרת לשימוש ב-Iterator** על ידי המהדר. מאחורי הקלעים נעשה שימוש במתודות `hasNext()` ו-`next()` לניהול האיטרציה.

**8\. מה היתרון המרכזי בשימוש ב-Generics ב-Java?** 

היתרון העיקרי הוא **הבטחת בטיחות טיפוסים (Type Safety) בזמן קומפילציה**. מנגנון זה מונע שגיאות של חוסר תאימות ו**חוסך את הצורך בביצוע Casting מפורש** בעת שליפת נתונים מאוספים.

**9\. מהו תפקידו של בלוק ה-finally בטיפול בחריגות?** 

מטרת הבלוק היא **הבטחת שחרור משאבים חיצוניים** כגון קבצים או מסדי נתונים. הייחודיות שלו היא ש**הוא מתבצע תמיד**, ללא קשר לשאלה האם התרחשה חריגה בבלוק ה-try.

**10\. מה ההבדל בין טווח ה-Singleton לטווח ה-Prototype בניהול Spring Beans?** 

בטווח **Singleton**, המהווה ברירת מחדל, **Spring מנהלת מופע יחיד של האובייקט** לכל אורך חיי היישום. בטווח **Prototype**, לעומת זאת, **מופע חדש נוצר בכל בקשה**, ו-Spring אינה מנהלת את שלב ההריסה שלו.

## **סיכום נושאים מורכבים יותר:**

**1. עקרון ההסתרה וצימוד רופף (Encapsulation and Loose Coupling):** 

תכנות מול ממשקים הוא נדבך קריטי ביצירת קוד גמיש. ממשק מגדיר "מה" ייעשה ללא חשיפת ה-"איך". שימוש בממשקים כמו List מאפשר החלפת מימושים מבלי לפגוע בקוד הלקוח. ב-Spring, מעבר לממשק SpellChecker אפשר הזרקת מימושים שונים ל-EmailClient ללא שינוי קוד, מה שמחזק את יכולת התחזוקה והבדיקה.  
ממשק פועל כחוזה המבטיח התנהגות מסוימת, ובכך מאפשר למערכות מורכבות להשתנות ולהתפתח מבלי לייצר תלות הדוקה במימוש ספציפי.

**2\. ניהול משאבים ושגיאות:** 

היררכיית ה-Throwable ב-Java מפרידה בין Error ל-Exception כדי להבחין בין כשלי מערכת קריטיים לשגיאות הניתנות לטיפול. Error מייצג קריסה חמורה ב-JVM שאינה בשליטת האפליקציה, בעוד ש-Exception מיועד להתמודדות מצד המפתח, בין אם בטיפול במצבי קצה או במניעת טעויות לוגיות מראש.  
הבנת הבחנה זו מאפשרת למפתחים לעצב אסטרטגיית טיפול בשגיאות המפרידה בין אירועים בלתי הפיכים לבין תקלות הדורשות התאוששות מסודרת.

**3\. התפתחות התחביר ב-Java:** 

המעבר ממחלקות פנימיות אנונימיות ל-Lambdas ורפרנסים למתודות סימן תמורה משמעותית בקריאות הקוד. ביטויי למדא מייצגים פונקציות בצורה מינימליסטית, ורפרנסים למתודות (::) מייעלים זאת אף יותר על ידי הפניה ישירה לפונקציות קיימות. שינויים אלו מעודדים כתיבה בסגנון פונקציונלי ומודרני.  
פשטות התחביר מאפשרת למפתחים להתמקד בלוגיקה העסקית במקום בקידוד מבני מסורבל, ובכך משפרת את הדינמיות של פיתוח ב-Java.

**4\. ה-Spring IoC Container כליבה של שלדת העבודה:** 

מנגנון ה-IoC מעביר את האחריות לניהול אובייקטים מהמפתח לקונטיינר. מחזור חיי ה-Bean כולל אתחול, הזרקת תלויות וניקוי משאבים. בעזרת Component Scanning ו-@Autowired, המערכת מאתרת ומזריקה תלויות באופן אוטומטי, מה שמקל משמעותית על תהליך הפיתוח והאינטגרציה.  
מנגנון זה מבטיח שהאפליקציה תהיה מודולרית וקלה להרחבה, תוך צמצום הצורך בניהול ידני של קשרי גומלין בין רכיבים.

**5\. ניידות וביצועים:** 

התהליך הדו-שלבי של Java מאפשר את עקרון "Write Once, Run Anywhere". המהדר מייצר Bytecode נייטרלי שאינו תלוי פלטפורמה, וה-JVM מתרגם אותו בזמן אמת לקוד מכונה מקומי. יתרון הניידות הוא עצום, אך הוא כרוך בעלויות פוטנציאליות במשאבי עיבוד ובזמן עליה בשל שכבת התרגום הנוספת.  
מבנה זה מבטיח עקביות בין סביבות עבודה שונות, תוך מתן מענה לאתגרי ההרצה על מערכות הפעלה מגוונות ללא צורך בהידור מחדש.

## 

## 

## 

## 

## 

## **מילון מונחים מקיף**

| מונח | הגדרה |
| ----: | ----: |
| **JDK (Java Development Kit)** | חבילת התוכנה הכוללת את הכלים הדרושים לפיתוח ב-Java, כולל הקומפיילר וה-JRE. |
| **JVM (Java Virtual Machine)** | המכונה הווירטואלית המריצה את ה-Bytecode ומאפשרת ל-Java להיות עצמאית מפלטפורמה. |
| **Bytecode** | הפלט של הקומפיילר (קובצי `.class`), פורמט ביניים שאינו קוד מכונה אך ניתן להרצה על ידי ה-JVM. |
| **Interface (ממשק)** | חוזה המגדיר אילו מתודות מחלקה צריכה לממש, ללא פירוט המימוש עצמו. |
| **Generic Class** | מחלקה המשתמשת בפרמטר טיפוס (כמו `<T>`), המאפשרת לה לעבוד עם טיפוסים שונים תוך שמירה על בטיחות בזמן קומפילציה. |
| **Boxing / Unboxing** | תהליך ההמרה האוטומטי בין טיפוס פרימיטיבי (כמו `int`) למחלקת העטיפה שלו (`Integer`) ולהיפך. |
| **Checked Exception** | חריגה שהקומפיילר מחייב לטפל בה (באמצעות `try-catch`) או להצהיר עליה (`throws`). |
| **Lambda Expression** | פונקציה אנונימית תמציתית המאפשרת להעביר התנהגות כפרמטר למתודה. |
| **Method Reference** | קיצור דרך לכתיבת למדא המפנה ישירות למתודה קיימת (לדוגמה: `System.out::println`). |
| **Spring Bean** | אובייקט Java המנוהל על ידי ה-Spring IoC Container. |
| **Dependency Injection (DI)** | דפוס שבו ה-Spring Container מזריק לאובייקטים את התלויות שלהם בזמן ריצה. |
| **Auto-wiring** | יכולת של Spring לזהות ולהזריק תלויות באופן אוטומטי מבלי שהמפתח יגדיר זאת ידנית ב-XML או ב-Java Config. |
| **Component Scanning** | מנגנון שבו Spring סורק חבילות (Packages) כדי למצוא מחלקות המסומנות ב-`@Component` ויוצר עבורן Beans. |
| **Stack Trace** | רשימת המתודות שנקראו עד לנקודה שבה קרתה השגיאה, משמש לניפוי שגיאות. |
| **Abstract Class** | מחלקה שאינה ניתנת למימוש ישיר (instantiation) ומשמשת כבסיס למחלקות אחרות. |
| **Tomcat** | שרת HTTP (Servlet Container) המשמש להרצת יישומי ווב מבוססי Java. הוא יכול להיות משובץ ביישום Spring Boot או לשמש כשרת חיצוני לפריסת קובצי WAR. |
| **WAR file (Web Application Archive)** | פורמט ארכיון (קובץ דחוס, סיומת \*.war) המשמש לפריסת יישומי ווב מבוססי Java לשרת יישומים חיצוני, כמו Tomcat. |
| **JAR (Java ARchive) file** | פורמט קובץ ארכיון (סיומת \*.jar) המשמש לאריזת קבצי מחלקה ומשאבים נלווים. הוא יכול לשמש כספרייה (dependency) או כיישום עצמאי הכולל שרת משובץ. |

